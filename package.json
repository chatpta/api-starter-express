{
  "name": "starter-api-express",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "export NODE_ENV=development && export PORT=3002 && node ./bin/www",
    "devstart": "export NODE_ENV=development && export PORT=3002 &&  nodemon ./bin/www",
    "production": "export NODE_ENV=production && node ./bin/www",
    "test": "export NODE_ENV=test && mocha --watch --reporter spec --bail --check-leaks test/**/*.test.js"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^10.0.0",
    "express": "~4.16.1",
    "helmet": "^4.6.0",
    "morgan": "~1.9.1",
    "pg": "^8.7.1"
  },
  "devDependencies": {
    "mocha": "^9.1.3",
    "nodemon": "^2.0.14",
    "supertest": "^6.1.6"
  }
}
